# Caddy — HTTPS для пилота ПрорАКТ 360 (пример, подставить свой домен).
# Прод-бот портов не занимает; пилот слушает 127.0.0.1:8444, наружу — только через Caddy.
tma360.example.com {
	reverse_proxy 127.0.0.1:8444
}
